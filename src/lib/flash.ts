import request = require("request");
import { Path } from "typescript";

export class Flash {
    constructor (version: string) {

    }

    /**
     * getFirmata
     */
    public async getFirmata(version: string = "latest") {
        
    }

    /**
     * flashFirmware
     */
    public flash(firmware?: Path) {
        if(!firmware){

        }
        else {
            
        }
    }
}